import React, { PropTypes } from 'react'
import sty from './Test.scss'

export const Test = () => (
  <div className={sty.container}>
    <h4>Test</h4>
  </div>
)

Test.propTypes = {
  // : PropTypes.
}

export default Test
